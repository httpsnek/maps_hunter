{
  "permissions": {
    "allow": [
      "WebFetch(domain:raw.githubusercontent.com)",
      "WebFetch(domain:github.com)",
      "Bash(git stash clear && \\\\\ngit reflog expire --expire=now --all && \\\\\ngit gc --prune=now --aggressive && \\\\\ngit log --all -p | grep \"sk-or-v1-\" | wc -l)"
    ]
  }
}
